# Changes 2026-09-24 - folder reorganisation

Branch `reorganize`, squash-merged into `main` as "Reorganizing data".

## `[CHANGED]` Repo layout

Files are grouped by kind instead of all sitting in the project root:

| Folder | Holds | Git |
|---|---|---|
| `config/` | files the user edits: `avoidable.json`, `nights.json`, `roster.txt`, `consumables.json`, `defensives.json` + the `*.example.json` templates | tracked except `nights.json` and `roster.txt` |
| `data/` | files the tool writes: `cache/` (was `.cache/`), `abilities_seen.json`, `mplus_history.json` | whole folder ignored |
| `dashboards/` | built HTML; now the **default output location** of `build_dashboard.py` (`-o` still overrides) | whole folder ignored (player names) |
| `docs/` | `CHANGES_*.md`, `ROADMAP_*.md`, `reviews/REVIEW_*.md`, `plans/` (was `docs/superpowers/plans/`) | tracked |

`CLAUDE.md`, `README.md`, the Python modules, `dash/`, `tests/`, `systemd/`, `legacy/` stay where they were.

## `[ADDED]` `path.py`

Single source of truth for the layout: `ROOT`, `CONFIG_DIR`, `DATA_DIR`, `DASHBOARD_DIR`. Creates `data/` and `dashboards/`
on import because both are git-ignored and therefore missing in a fresh clone.

`[DECISION]` Named `path.py` (not `paths.py`); import names explicitly (`from path import DATA_DIR`) - `build_dashboard.py`
has a local variable called `path`, so a bare `import path` there would be shadowed.

## `[CHANGED]` Modules now import their locations from `path.py`

- `cache.py`: `CACHE_DIR = data/cache`
- `collect_data.py`: `NIGHTS_FILE`, `CONSUMABLES_FILE`, `DEFENSIVES_FILE` -> `config/` (`HERE` kept; still used internally)
- `mplus.py`: `ROSTER_FILE` -> `config/`, `HISTORY_FILE` -> `data/`; `HERE` removed
- `dash/common.py`: `AVOIDABLE_FILE` -> `config/`, `MPLUS_FILE` -> `data/`; `HERE` removed
- `build_dashboard.py`: `abilities_seen.json` -> `data/`; default output -> `dashboards/`; no longer imports `HERE`
- `systemd/wcl-dashboard.sh`: `OUT=dashboards/...` (the script already `cd`s to the project root)

## `[CHANGED]` `.gitignore`

Folder rules (`data/`, `dashboards/`) replace the per-file list (`.cache/`, `dashboard_*.html`, `serenity-*.html`, `out/`,
`abilities_seen.json`, `mplus_history.json`). Per-guild config is ignored by path under `config/`.
`[DECISION]` Only `nights.json` (report codes) and `roster.txt` (player names) are ignored. `avoidable.json`,
`consumables.json` and `defensives.json` are tracked - they hold ability names, not guild or player data.

## Verification

- `python -m pytest -q`: 20 passed.
- `python build_dashboard.py 2026-09-01 2026-09-21 --no-mplus`: 0 API calls, 674 cache hits (the moved cache was found),
  output written to `dashboards/`, nothing new created in the root.

## `[TODO]` Not done on purpose

Moving the root modules into a `wcl/` package. It is a code change (12 import lines, `python -m wcl.mplus`, systemd script,
test `sys.path` bootstrap), not a data reorganisation, so it belongs in its own branch.

## `[ADDED]` UX audit of the built dashboard folded into the redesign plan

- Ran the ui-ux-pro-max review over `dashboards/dashboard_2026-08-19_to_2026-09-21.html` (1440 px and 390 px captures of Home, a
  Heroic boss tab and the reference design; WCAG contrast maths for the current palette and the planned tokens; guideline searches for
  contrast, keyboard, deep links, sticky nav, loading feedback, empty states, heading hierarchy, type scale and chart selection).
- `docs/plans/2026-09-24-dashboard-redesign.md`: new section **Audit of the 2026-09-24 build** (21 findings, ranked, each mapped to a
  task); *Audit amendments* block appended to Tasks 1-9; new **Task 10** (density defaults: top-N tables, capped box plot, collapsed
  secondary tables), **Task 11** (status without colour: HP bands with swatches, prep glyphs, visible "review" tag), **Task 12**
  (mobile: toolbar as a `<details>` disclosure, not sticky); Review Focus items 6-7; File Structure and Self-Review updated.
- `[FIXED]` The plan's *No git repository* constraint was stale; it now asks for a `redesign` branch with one commit per task plus the
  tagged CHANGES entry.
- `[CAVEAT]` `firefox --headless --screenshot` fires on `load`, before the async payload inflation activates a `#tabN` deep link, so plain
  deep-link captures show Home. The plan documents the workaround (a scratchpad copy with an `async` script to an unroutable address that
  delays `load` by ~90 s). This is also audit finding A1: the real page has no loading state.
- `[DECISION]` The skill's design-system output (Fira Sans / Fira Code) was not adopted: fonts stay the system stack (file:// + Discord).
- No code changed in this step.

# Redesign (branch `redesign`, plan `docs/plans/2026-09-24-dashboard-redesign.md`)

Design reference: `dashboards/serenity-dashboard-rebuilt.html`. One entry per task; commits carry the same text.

## Task 1 — design tokens
- `[ADDED]` `dash/static/tokens.css`: surfaces, ink, accent, status, six series colours, type scale, spacing, radius, focus ring, font stacks. Inlined before `dash.css`.
- `[ADDED]` `dash.common.load_tokens()` (regex over the CSS file, fails loudly on missing keys) and a `TOK` object in `dash.js` read through `getComputedStyle`. `[DECISION]` One file holds every design value; Python and JS read it instead of carrying their own hex literals.
- `[ADDED]` `--border-control` (`#5a6a86`, 3.2:1 on the card) for chip / pull-box / tab / select borders — audit item 9: `--border` and `--border-strong` are decorative (1.4:1 and 1.8:1). `tests/test_tokens.py::test_tokens_contrast` asserts text tokens ≥ 4.5:1 on card and raised, `border-control` ≥ 3:1, series ≥ 3:1.
- `[CHANGED]` `dash.css` variables renamed to the token names (`--panel` → `--surface-card`, `--muted` → `--ink-dim`, `--blue` → `--series-1`, ...). Base type 15 px / 1.5 from `--font`, tabular numerals everywhere.
- `[CHANGED]` Focus ring is a two-ring box-shadow (`--ring`) instead of a yellow outline. Plotly layout font/grid/colorway come from `TOK`; default top margin 20 (titles move to HTML headings in Task 7).
- `[CAVEAT]` `tests/test_dashboard.py::test_css_uses_type_scale` is `xfail` until Task 8 rewrites `dash.css` onto `var(--fs-*)`.
- Tests: 24 passed, 1 xfailed. Fixture boss tab rendered headless without `#jsError`.
- `[FIXED]` (review of Task 1) `chartLayout()` — a pure, quickjs-tested helper inside `// --- pure chart helpers ---` markers — keeps the 50 px top margin for charts that still draw a Plotly in-canvas title, so titles do not overlap the plot until Task 7 moves them into HTML headings. Callers with their own `margin` are untouched.
- `[FIXED]` (review of Task 1) Focus ring: `outline:2px solid transparent` instead of `outline:none` (forced-colors / Windows High Contrast drops `box-shadow` but recolours a transparent outline), and sortable `th` get an inset ring because `overflow:hidden` tables clip the outer one. Tests: `test_chart_layout_keeps_room_for_plotly_titles`, `test_focus_ring_survives_forced_colors_and_table_headers`. Suite: 26 passed, 1 xfailed.
- `[TODO]` From the review (deferred, minor): drop `border-radius:4px` from the global focus rule; strip `/* */` comments before the token regex and document that `load_tokens()` returns unresolved CSS text (`--ring` contains `var()`); `tok()` should report a missing token in `#jsError` instead of returning `''`; colorway is 6 series so a boss pulled on 7+ nights repeats colour+marker (dash still differs); CLAUDE.md §2/§10 to mention `tokens.css` / `load_tokens()` (batched into Task 9); the CHANGES heading `# Redesign` should be `## Redesign`.
