# secrets
.env
*.token
*.token.json

# python
venv/
__pycache__/
.pytest_cache/

# generated state and build output (data/ holds the WCL cache; dashboards contain player names)
data/
dashboards/

# per-guild config (player names / report codes); everything else in config/ is tracked
config/nights.json
config/roster.txt
